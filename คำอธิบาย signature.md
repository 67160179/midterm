# ทำไมแก้ Token นิดเดียวก็ใช้ไม่ได้แล้ว (เรื่อง Signature)

จากขั้นตอน 3.7 ที่ลองแก้ตัวอักษรใน token แล้วยิงไปที่ `/api/v1/auth/me` แล้วได้ `401 INVALID_TOKEN` กลับมา — มาดูกันว่าทำไมมันถึงรู้ทันว่าเราแอบแก้

## Token หน้าตายังไง

JWT ที่เราได้ตอน login จะมี 3 ท่อนคั่นด้วยจุด

```
Header.Payload.Signature
```

- **Header** — บอกว่าใช้ algorithm อะไรเซ็น
- **Payload** — ข้อมูลที่ใส่ไป ในโปรเจกต์นี้คือ `id`, `email`, `role`
- **Signature** — ตัวที่เป็นพระเอกของเรื่องนี้

สิ่งที่ต้องรู้ไว้ก่อนคือ Header กับ Payload **ไม่ได้ถูกเข้ารหัสปิดบัง** มันแค่ถูก encode เป็น base64 เฉยๆ ใครเอา token ไปแปะที่ jwt.io ก็อ่านออกได้เลยว่าข้างในมีอะไร นี่คือเหตุผลว่าทำไมห้ามยัดรหัสผ่านหรือข้อมูลลับลงไปใน payload

## Signature มาจากไหน

ตอนเซิร์ฟเวอร์สร้าง token (ฟังก์ชัน `generateToken` ใน `auth-helpers.js`) มันจะเอา Header+Payload ไปเข้าสูตรแฮชร่วมกับ `JWT_SECRET` ที่อยู่ในไฟล์ `.env`

พูดง่ายๆ Signature คือ "รอยนิ้วมือ" ที่คำนวณมาจากเนื้อหา token + กุญแจลับที่มีแค่เซิร์ฟเวอร์เท่านั้นที่รู้ ไม่มีใครนอกเซิร์ฟเวอร์รู้ค่า secret นี้เลย

## ทีนี้พอแก้ token แล้วเกิดอะไรขึ้น

ทุกครั้งที่มี request เข้ามา middleware `authenticateToken` จะทำสองอย่าง

1. แกะ token ที่ได้รับออกมา
2. **คำนวณ Signature ใหม่** จาก Header+Payload ที่ได้รับ แล้วเอาไปเทียบกับ Signature เดิมที่แนบมา

ถ้าเราแอบแก้ตัวอักษรใน payload สักตัว (เช่นพยายามเปลี่ยน role จาก student เป็น admin) Signature ที่แนบมาใน token มันยังเป็นค่าเดิม (คำนวณจาก payload เก่า) แต่พอเซิร์ฟเวอร์เอา payload ใหม่ที่เราแก้ไปคำนวณ Signature ใหม่ ค่าที่ได้จะไม่ตรงกับของเดิมเลย เพราะฟังก์ชันแฮชมันมีสมบัติแบบว่า **แก้แค่นิดเดียวผลลัพธ์เปลี่ยนไปคนละเรื่องเลย** (ยิ่งเดาว่าจะเปลี่ยนนิดเดียวยิ่งเดาไม่ถูก)

พอเทียบแล้วไม่ตรง `jwt.verify()` ก็ throw error ออกมา เซิร์ฟเวอร์เลยตอบ

```json
{ "error": { "code": "INVALID_TOKEN", "message": "Token ไม่ถูกต้องหรือหมดอายุ" } }
```

พร้อม status `401` ตามที่เจอในแล็บ

## แล้วทำไมปลอม Signature เองไม่ได้ล่ะ

เพราะการคำนวณ Signature ต้องใช้ `JWT_SECRET` ซึ่งอยู่แค่ในไฟล์ `.env` ฝั่งเซิร์ฟเวอร์เท่านั้น ต่อให้เดา payload ถูกทุกตัวอักษร แต่ไม่รู้ secret ก็คำนวณ Signature ที่ถูกต้องไม่ได้อยู่ดี

เพราะงั้น `JWT_SECRET` ถึงต้องเป็นค่าที่สุ่มยาวๆ และเก็บเป็นความลับจริงจัง (ห้าม commit ขึ้น GitHub เด็ดขาด) เพราะความปลอดภัยทั้งระบบแขวนอยู่กับความลับของค่านี้ค่าเดียว

## สรุปสั้นๆ

JWT ไม่ได้ปิดบังข้อมูลข้างใน (อ่านออกได้อยู่แล้ว) แต่มันการันตีว่า **ข้อมูลข้างในไม่ถูกแก้** ถ้าถูกแก้เมื่อไหร่ Signature จะไม่ตรงกันทันที เซิร์ฟเวอร์เลยจับได้และปฏิเสธ token นั้นไปเลย